#include "holberton.h"

/**
 * alloc_grid - desc
 * @width: desc
 * @height: desc
 * Return: return
 */
int **alloc_grid(int width, int height)
{
	int **dptr;

	if (width < 1 || height < 1)
		return (NULL);

	if (dprt == NULL)
		return (NULL);

	return (dptr);
}

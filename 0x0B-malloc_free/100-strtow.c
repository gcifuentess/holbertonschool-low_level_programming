#include "holberton.h"

/**
 * strtow - desc
 * @str: desc
 *
 * Return: desc
 */
char **strtow(char *str)
{
	char **dprt;

	if (str == NULL || str == "")
		return (NULL);

	dptr = malloc(1);

	if (dptr == NULL)
		return (NULL);

	return (dptr);
}
